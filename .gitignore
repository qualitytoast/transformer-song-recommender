# --- Data (Million Playlist Dataset — too large for GitHub) ---
data/

# --- Old single-file checkpoints, from before the bundle format. Root only:
# --- artifacts/ IS committed, because the host builds the serving image straight
# --- from this repo and the weights have to ship with it.
/*.npz

# --- Training log ---
training_log.txt

# --- Python virtual environment ---
.venv/
venv/

# --- Python caches ---
__pycache__/
*.py[cod]
*$py.class

# --- Generated plots / outputs (optional, uncomment if you don't want them tracked) ---
# *.png

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Experiment run logs ---
runs/
